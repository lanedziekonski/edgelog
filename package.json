{
  "name": "edgelog",
  "version": "1.0.0",
  "description": "EdgeLog trading journal — see backend/ for the deployable app",
  "private": true,
  "scripts": {
    "start": "cd backend && node server.js",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
