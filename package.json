{
  "name": "edgelog",
  "version": "1.0.0",
  "description": "EdgeLog trading journal",
  "scripts": {
    "start": "node backend/server.js",
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
